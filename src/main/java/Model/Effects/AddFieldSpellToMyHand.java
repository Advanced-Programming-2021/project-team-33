package Model.Effects;

import Model.Card;
import Model.Effect;

public class AddFieldSpellToMyHand implements Effect {
    @Override
    public void run(Card card) {
        // add field spell from deck to hand
    }
}
